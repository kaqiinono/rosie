* 帮我把整个项目改成nextjs+react+tsx+Tailwind CSS的架构，所有依赖使用最新版本
* 样式必须支持响应式（移动端适配）
* 使用 Tailwind CSS v4 替换所有原始样式
* 这个项目需要按照模块最小化进行拆分，对项目进行模块化，细粒度越小越好，方便调试，比如方法都放在utils目录下根据功能命名，常量都放在utils/constant.ts文件里，hook都放在hooks目录下，组件都放在components目录下根据用途命名，类型声明都放在utils/type.ts下
* 这个项目最终会打包成一个pwa应用并部署到vercel上
* 移动端访问这个页面的时候还是很慢，请在每个点击则跳转页面的按钮在点击时都给页面加一个loading状态直到跳转完成。
* 请保留现有的路径跳转，针对现有的页面路径进行路由设计
