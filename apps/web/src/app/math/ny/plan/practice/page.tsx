'use client'

import MathPlanPageShell from '../MathPlanPageShell'

/** Practice entry — jump into today's first unfinished required problem. */
export default function MathPlanPracticePage() {
  return <MathPlanPageShell autoStart />
}
