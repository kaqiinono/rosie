/** Fixed viewport shell for flipbook reader (loading, error, and active reader). */
export const FLIPBOOK_READER_SHELL_CLASS =
  'flipbook-reader fixed inset-0 z-40 flex max-h-none flex-col overflow-hidden bg-[var(--flipbook-bg)] pt-[env(safe-area-inset-top,0px)] pb-[env(safe-area-inset-bottom,0px)] pl-[env(safe-area-inset-left,0px)] pr-[env(safe-area-inset-right,0px)]'
