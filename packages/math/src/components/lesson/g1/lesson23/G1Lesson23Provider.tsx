'use client'

import { createLessonProvider } from '@rosie/math/components/shared/createLessonProvider'

const { Provider, useLessonContext } = createLessonProvider('G1Lesson23')

export default Provider
export { useLessonContext as useG1Lesson23 }
