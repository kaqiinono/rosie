/// <reference types="styled-jsx" />
// Pulls in styled-jsx's global JSX augmentation so `<style jsx>` is typed when
// this package (and the @rosie/english / @rosie/flipbook source it pulls in) is
// type-checked in isolation (the app gets this transitively via Next).
